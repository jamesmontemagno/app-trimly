name: macOS Unit Tests

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

permissions:
  contents: read

jobs:
  tests-macos:
    runs-on: macos-26

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Select Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: '26.6'

      - name: Build macOS app
        run: xcodebuild -scheme TrimTally -destination "platform=macOS,arch=arm64" CODE_SIGNING_ALLOWED=NO build

  tests-ios:
    runs-on: macos-26

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Select Xcode
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: '26.6'

      - name: Build iOS app
        run: xcodebuild -scheme TrimTally -destination "platform=iOS Simulator,name=iPhone 17" CODE_SIGNING_ALLOWED=NO build

      - name: Run iOS unit tests
        run: xcodebuild -scheme TrimTally -destination "platform=iOS Simulator,name=iPhone 17" CODE_SIGNING_ALLOWED=NO test
